package _ARCHIVE.Polymorphism;
/* we could very well use a regular class instead of an abstract one
however for seeing the real usefulness of abstract will use abstract class
abstract class cannot be instantiated so we dont instantiated it
only subclasses can be created from abstract class
only abstract class can have abstract method */

abstract public class Vehicle {
    
    abstract public void go();
}
